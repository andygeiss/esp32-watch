build/
lvgl/
compile_commands.json
.DS_Store

# ESP-IDF: build output, the downloaded driver components, and the generated
# sdkconfig. dependencies.lock is checked in — it is what pins them.
firmware/build/
firmware/managed_components/
firmware/sdkconfig
firmware/sdkconfig.old

# The clip KAI borrows its voice from, and what it says. A recording of a
# person, so it stays out of a licensed repository; voice.c does without it.
voices/

# Where the speech server's address and its API key go, read by `make run`.
# One machine's setup, and one of the four lines is a secret.
.env

# clangd writes its index here as a side effect of compile_commands.json.
.cache/
